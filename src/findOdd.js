function findOdd(table) {
    let acc =[];
    for(number of table){
    if (table.length==1) {
        acc.push(number)
    }
    }
    return acc
}

let table = [1,2,2];
console.log(findOdd(table));

module.exports={
    findOdd
}
